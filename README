# MUI App Boilerplate

The goal is to provide a node / React base application built on Material-UI. It includes a complete workflow including authentication, permission management and fast developpment tools.

 * [The backend GraphQL API](backend) uses Sequelize, Apollo Graphql Server, Sequelize GraphQL Server
 * [The frontend web app](frontend) is based on React, MUI, MUI CRUDF
 * [A firefox extension](browser extension) demonstrates how to benefits from the same capabilities than the front app but in a ready to use extension, injecting information in (and retrieving from) Google search
